#coding=utf-8
#from sys import argv #从系统里导入包
#script,a=argv #命令行中赋值
#print '\n\r\t %s%d%r ' % (a,b,c) #格式化内容，打印输出
# raw_input('>') #默认都是字符串，如果需要转化，前面加int(raw_input())
# input('>') #支持不同格式，如果是字符串需要加引号。直接输入数字无需用int转化
# open('file','w') #打开文件，文件名.类型，w只写，r只读，用w+可读写。w状态可创建新文件，清空已有内容，不支持read
# read() #读文件内容，可将内容赋给变量，之后还需要执行close。如果打开文件不赋给变量就无所谓。
#print open('a.txt','r').read() #打印文件内容
"""
def add(a,b):
	print '%d加上%d 等于' % (a,b) #定义函数，变量
	return a + b #返回值
	
c = add (1,2)
"""	